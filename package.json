{
  "name": "agent-threads",
  "version": "0.1.0",
  "description": "Read-only local CLI for searching and reading agent session history",
  "private": true,
  "type": "module",
  "bin": {
    "agent-threads": "./src/index.ts",
    "ath": "./src/index.ts"
  },
  "scripts": {
    "dev": "bun run ./src/index.ts",
    "test": "bun test",
    "check": "bun test"
  },
  "engines": {
    "bun": ">=1.3.0"
  },
  "dependencies": {
    "cac": "^7.0.0",
    "zod": "^4.3.6"
  }
}
